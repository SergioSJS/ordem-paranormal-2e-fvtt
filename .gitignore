# Nunca commitar o PDF do playtest (material protegido)
*.pdf
docs/*.pdf

node_modules/
.DS_Store
*.DS_Store

# CSS gerado pelo sass
styles/*.css
styles/*.css.map

# Preview gerado por scripts/preview.mjs
preview.html

# Screenshot do e2e quando rodado sem pasta de saída (npm run e2e)
e2e-foundry.png

# Packs compilados (fonte fica em packs/sources/)
packs/habilidades/
packs/perfis/

.fvtt-config
.vscode/settings.json
