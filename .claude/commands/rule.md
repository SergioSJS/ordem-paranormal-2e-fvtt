---
description: Consulta uma regra na spec do playtest
argument-hint: <termo ou seção, ex.: "RA" ou "6.3">
---

Consulte `$1` em `docs/op2-playtest-spec-foundry.md`.

Responda com:

- O que a regra diz, **citando a seção**.
- Se é ambígua ou contraditória, diga onde e aponte a entrada de `docs/LACUNAS.md`.
- Onde ela está implementada hoje (arquivo e função), ou "ainda não implementada".
- O que o playtest **não** define sobre isso.

Não proponha implementação a menos que eu peça.
